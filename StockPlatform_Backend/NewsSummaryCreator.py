from markdown_pdf import MarkdownPdf

pdf = MarkdownPdf()


#Summarized Data will be an array of items
def createSummary(pdf_md, stock_choice, summarized_data):
    pdf.add_section(
        Section(

        )
    )